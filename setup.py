# from setuptools import setup, find_packages
# setup(name = 'ab_test_permutation_test', packages = find_packages())
# setup(name = 'analytics', packages = find_packages())
# setup(name = 'generator', packages = find_packages())
# setup(name = 'interface', packages = find_packages())
# setup(name = 'utils', packages = find_packages())
